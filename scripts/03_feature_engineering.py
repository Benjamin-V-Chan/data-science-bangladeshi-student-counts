# Load necessary libraries
# Read the cleaned dataset
# Create new features based on age distribution
# Aggregate data at school level (e.g., average age per class)
# Save feature-engineered dataset for modeling in 'outputs/'
